import css from 'styled-jsx/css'

export const style =  css`
	button{
        background: var(--primary-color);
        padding: 10px 15px;
        border-radius: 10px;
        border: none;
	}
    button:hover {
        opacity: 0.8;
        cursor: pointer;
    }
`